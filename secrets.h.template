#pragma once

// Copy this file to d:\Projects\secrets.h and fill in your credentials.
// It lives outside the repo so it is never committed.

#define WIFI_SSID     "your_ssid_here"
#define WIFI_PASSWORD "your_password_here"
